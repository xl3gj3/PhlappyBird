var RendererFactory = function(model){
    const renderer = {};

    renderer.render = function() {
        // loop through model
        // get individual shapeObj
        // do shapeObj.draw();
    }

    return renderer;
}